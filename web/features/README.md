# 主页「特色」配置

编辑同目录 **`features.json`**。`GET /api/features` 读取；无 Node 时可请求 `/features/features.json`。

## JSON 结构

```json
{
  "section": { "title": "", "subtitle": "" },
  "tabs": [{ "title": "", "body": "" }]
}
```
