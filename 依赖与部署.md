# 依赖与部署（概要）

## 运行环境

- **Node.js**：建议 **18+ LTS**（项目为 `server/server.mjs` 的 ES Module，需原生支持 `import`）。
- **npm**：随 Node 安装即可。仓库根目录执行依赖安装；**不要**把 `.tools/node/` 提交进 Git（已在 `.gitignore`）。若本机放有 `.tools/node/node.exe`，`run.bat` 会优先使用，非必需。

## 依赖（npm）

根目录 `package.json` 主要包含：

| 包 | 用途 |
|----|------|
| `express` | HTTP 服务、静态资源与管理端路由挂载 |
| `express-session` | 管理端登录会话 |
| `marked` | 百科等 Markdown 渲染 |
| `multer` | 管理端上传（如百科附件） |
| `@picocss/pico` | 样式相关依赖 |

安装：

```bash
npm ci
```

（日常开发也可用 `npm install`。）

## 本地启动

```bash
npm start
```

等价于 `node server/server.mjs`。Windows 也可直接运行根目录 **`run.bat`**。

- 默认监听 **`0.0.0.0:8080`**。
- 可用环境变量 **`PORT`**、**`HOST`** 覆盖端口与绑定地址。

## 部署要点

1. **进程**：生产环境直接运行 `node server/server.mjs`，或使用进程管理器（如 systemd、PM2）保活。
2. **反向代理**：前面用 Nginx / Caddy 等做 **HTTPS** 与域名，反代到本服务 `PORT`。
3. **安全**：管理端主账号与密码哈希当前写在 `server/admin-auth-store.mjs` 中，**上线前务必改为自己的账号与哈希**，并限制管理路径访问来源（IP 白名单、VPN 等）。
4. **数据**：百科与管理端会写入 `web/wiki/` 等目录下文件，注意**磁盘权限、备份与防误删**。

## 模块说明（内容目录）

- **后台总览**（各 Tab 与落盘路径）：`web/admin/README.md`
- **分模块说明**（与 `web/staff/README.md` 同风格）：`web/wiki/`、`web/events/`、`web/features/`、`web/join-guide/`、`web/staff/` 下的 `README.md`
